% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe_here.R
\name{write_safe_here_marker}
\alias{write_safe_here_marker}
\title{Écrit le marqueur `.safe_here-root` dans une copie temporaire}
\usage{
write_safe_here_marker(temp_dir, origin_root)
}
\arguments{
\item{temp_dir}{`[character(1)]`\cr Racine de la copie temporaire.}

\item{origin_root}{`[character(1)]`\cr Racine réelle du projet (résultat
de [here::here()] appelé depuis le dossier source, avant copie), ou
`NA_character_`.}
}
\value{
Invisiblement `NULL`.
}
\description{
Appelée par `render_folder_worker()` juste après avoir copié le dossier
ciblé dans son répertoire temporaire de rendu. N'écrit rien si
`origin_root` est `NA` (racine introuvable même dans l'arborescence
réelle) : [safe_here()] se comportera alors, dans la copie, exactement
comme [here::here()] — même échec, pas de régression.
}
\keyword{internal}
