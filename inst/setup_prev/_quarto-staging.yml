# Écrite/synchronisée automatiquement par render_prev()/stage_prev() ; lue
# par les extensions ofce-quarto-extensions pour le bandeau "Version
# provisoire — non publiée".
draft: true

website:
  site-url: "https://staging.ofce.fr/"
  site-path: "prevYYMM/v0"

version: "v0"

comments:
  hypothesis: true

encrypt_site: true

project:
  output-dir: "_site_staging"
