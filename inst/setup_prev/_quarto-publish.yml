# Écrite/synchronisée automatiquement par render_prev()/publish_prev().
draft: false

website:
  site-path: "prev/prevYYMM"

comments:
  hypothesis: false

encrypt_site: false

project:
  output-dir: "_site_publish"
