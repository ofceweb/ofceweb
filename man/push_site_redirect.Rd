% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site_redirect.R
\name{push_site_redirect}
\alias{push_site_redirect}
\title{Pousse la page de redirection vers la version courante d'un site OFCE}
\usage{
push_site_redirect(path = ".", progress = TRUE, trigger = TRUE)
}
\arguments{
\item{path}{Chemin vers la racine du dépôt. Défaut `"."`.}

\item{progress}{Logique. Affichage de la progression. Défaut `TRUE`.}

\item{trigger}{Logique. Si `TRUE` (défaut), déclenche `ftp_redirect.yml`
après le push via [trigger_action()].}
}
\value{
Invisible `NULL`. Appelée pour ses effets de bord.
}
\description{
Génère un `index.html` de redirection pointant vers la version courante du
site (lue depuis le `site-path` du `_quarto.yml`) et le pousse sur la
branche `site-redirect`, puis déclenche le workflow `ftp_redirect.yml` pour
publier la page sur le serveur FTP.
}
\details{
Sans effet (sortie silencieuse) si `site-path` ne contient pas de segment
de version (`/v[0-9]+`) ou si `ofce_host` n'est pas `true`.

Appelée automatiquement par [site_version_up()] lors d'un incrément de
version, et par [stage_site()] à chaque déploiement staging.
}
\section{Site Users}{

}

\seealso{
[site_version_up()], [stage_site()], [deploy_site()]
}
