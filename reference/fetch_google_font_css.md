# Récupère le CSS Google Fonts d'une famille

Essaie successivement les variantes demandées, de la plus riche
(italiques + graisses) à la plus simple, car toutes les familles ne les
proposent pas.

## Usage

``` r
fetch_google_font_css(font)
```
