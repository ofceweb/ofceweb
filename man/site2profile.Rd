% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy_prev.R
\name{site2profile}
\alias{site2profile}
\title{Déploie un profil personnalisé vers staging/{repo}/{profile}/}
\usage{
site2profile(
  path = ".",
  profile,
  progress = TRUE,
  trigger = TRUE,
  full_deploy = FALSE
)
}
\arguments{
\item{path}{Chemin vers la racine du dépôt. Défaut `"."`.}

\item{profile}{Nom du profil Quarto (doit correspondre à un fichier
`_quarto-{profile}.yml` dans le dépôt).}

\item{progress}{Logique. Affichage de la progression. Défaut `TRUE`.}

\item{trigger}{Logique. Déclenche `ftp_deploy_profile.yml` après le push.
Défaut `TRUE`.}

\item{full_deploy}{Logique. Si `TRUE`, force la ré-émission complète vers
le FTP. Défaut `FALSE`.}
}
\value{
Invisible `NULL`.
}
\description{
Wrapper de [site2branch()] pour les profils Quarto qui ne sont ni
`"staging"` ni `"publish"`. Pousse `_site_{profile}/` vers la branche
`site-{profile}` et déclenche le workflow `ftp_deploy_profile.yml` en lui
passant le nom du profil en entrée. Le FTP cible est
`staging/{repo}/{profile}/` — sans numéro de version, le profil jouant ce
rôle.
}
\section{Prévision Users}{

}

\seealso{
[deploy_prev()], [site2branch()], [site2staging()]
}
