% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview_qmd.R
\name{preview_qmd}
\alias{preview_qmd}
\alias{preview_qmd_staging}
\title{Prévisualise le document `.qmd` actif dans RStudio}
\usage{
preview_qmd(profile = NULL, daemon = TRUE, ...)

preview_qmd_staging(daemon = TRUE, ...)
}
\arguments{
\item{profile}{`[character(1)]` ou `NULL`.\cr
Profil Quarto à utiliser pour le rendu (ex. `"staging"`, `"publish"`).
Passé à [quarto::quarto_render()] et utilisé pour lire le bon
`_quarto-{profile}.yml`. `NULL` (défaut) = rendu sans profil.}

\item{daemon}{Logique. Si `TRUE` (défaut), le serveur HTTP tourne en
arrière-plan sans bloquer la console.}

\item{...}{Arguments supplémentaires passés à [quarto::quarto_render()].}
}
\value{
Invisible `NULL`. Appelée pour ses effets de bord.
}
\description{
Récupère le chemin du document actif dans l'éditeur RStudio, vérifie qu'il
s'agit d'un fichier `.qmd`, le rend via [quarto::quarto_render()] puis
lance un serveur HTTP local avec [servr::httd()] positionné directement sur
le fichier HTML produit.
}
\details{
Le répertoire servi et le chemin initial sont déduits automatiquement :
\enumerate{
  \item Si un `_quarto.yml` est trouvé dans un dossier parent, la racine du
    projet est identifiée.
  \item Si `profile` est non-`NULL`, `_quarto-{profile}.yml` est lu pour en
    extraire `project.output-dir`.
  \item Sans projet ou sans `output-dir` configuré, le serveur pointe sur le
    dossier du `.qmd` lui-même.
}
}
\section{Prévision Users}{

}

