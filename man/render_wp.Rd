% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_wp.R
\name{render_wp}
\alias{render_wp}
\title{Rendu complet d'un document de travail (WP) OFCE}
\usage{
render_wp(
  path = ".",
  check_repo = TRUE,
  check = TRUE,
  progress = TRUE,
  render_site = TRUE,
  site2branch = FALSE,
  trigger = site2branch,
  workers = 8L
)
}
\arguments{
\item{path}{Chemin vers la racine du dépôt. Défaut `"."`.}

\item{check_repo}{Logique. Si `TRUE` (défaut), vérifie l'état du dépôt git
avant le rendu via [check_repo_status()].}

\item{check}{Logique. Si `TRUE` (défaut), appelle [check_wp()] avant le
rendu et abandonne si des erreurs bloquantes sont détectées.}

\item{progress}{Logique. Affichage de la progression. Défaut `TRUE`.}

\item{render_site}{Logique. Si `TRUE` (défaut), lance un serveur HTTP local
via [servr::httw()] sur `_site/` après le rendu.}

\item{site2branch}{Logique. Si `TRUE`, pousse `_site/` vers la branche de
déploiement via [site2branch()]. Défaut `FALSE`.}

\item{trigger}{Passé à [site2branch()]. Défaut = valeur de `site2branch`.}

\item{workers}{Entier. Nombre de workers parallèles pour le rendu. Défaut
`8L`.}
}
\value{
Invisible : sortie de [gert::git_status()].
}
\description{
Orchestre le build complet d'un WP Quarto : vérification du dépôt git,
vérification de la structure WP, nettoyage de `_site/`, rendu Quarto
(HTML + PDF), construction du sitemap, patch des hashes Bootstrap,
écriture du manifeste, synchronisation de la variable GitHub Actions
`FTP_SERVER_DIR` (WPs publiés uniquement), et optionnellement déploiement
sur la branche de déploiement et prévisualisation locale.
}
\section{Working Paper (WP) Users}{

}

\seealso{
[setup_wp()], [check_wp()], [deploy_wp()]
}
