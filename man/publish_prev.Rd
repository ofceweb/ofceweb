% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_prev.R
\name{publish_prev}
\alias{publish_prev}
\title{Rend et publie la prévision (publish)}
\usage{
publish_prev(
  path = ".",
  progress = TRUE,
  site2branch = TRUE,
  trigger = site2branch,
  full_deploy = FALSE,
  preview = FALSE,
  workers = 8L
)
}
\arguments{
\item{path}{Chemin vers la racine du dépôt. Défaut `"."`.}

\item{progress}{Logique. Affichage de la progression. Défaut `TRUE`.}

\item{site2branch}{Logique. Si `TRUE` (défaut), appelle [site2staging()]
après le rendu.}

\item{trigger}{Passé à [site2staging()]. Défaut = valeur de `site2branch`.}

\item{full_deploy}{Passé à [site2staging()]. Défaut `FALSE`.}

\item{preview}{Logique. Si `TRUE`, lance un serveur HTTP local via
[servr::httw()] sur le répertoire de sortie après le rendu. Défaut `TRUE`}

\item{workers}{Entier. Nombre de workers parallèles. Défaut `8L`.}
}
\value{
Invisible `NULL`.
}
\description{
Enchaîne [render_prev()] avec le profil `"publish"` puis pousse
`_site_publish/` vers la branche `site-publish` via [site2publish()].
}
\seealso{
[render_prev()], [deploy_prev()], [stage_prev()]
}
