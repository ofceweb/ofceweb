% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy_prev.R
\name{deploy_prev}
\alias{deploy_prev}
\title{Déploie la prévision OFCE (staging ou publish)}
\usage{
deploy_prev(
  path = ".",
  profile = "staging",
  progress = TRUE,
  trigger = TRUE,
  full_deploy = FALSE
)
}
\arguments{
\item{path}{Chemin vers la racine du dépôt. Défaut `"."`.}

\item{profile}{`"staging"` (défaut), `"publish"`, ou tout autre profil
Quarto déclaré dans `_quarto.yml`. Les profils personnalisés sont déployés
vers `staging/{repo}/{profile}/` sans numéro de version.}

\item{progress}{Logique. Affichage de la progression. Défaut `TRUE`.}

\item{trigger}{Logique. Déclenche le workflow GitHub Actions FTP après le
push. Défaut `TRUE`.}

\item{full_deploy}{Logique. Si `TRUE`, force la ré-émission de tous les
fichiers vers le FTP (ignore l'état incrémental). Défaut `FALSE`.}
}
\value{
Invisible `NULL`.
}
\description{
Pousse le répertoire de sortie déjà rendu vers la branche git appropriée
sans relancer le rendu Quarto. Pour staging, pousse `_site_staging/` vers
`site-staging` ; pour publish, pousse `_site_publish/` vers `site-publish`.
}
\seealso{
[stage_prev()], [publish_prev()], [site2staging()]
}
