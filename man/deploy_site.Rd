% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy_site.R
\name{deploy_site}
\alias{deploy_site}
\title{Déploie le site selon l'hébergement déclaré dans `_quarto.yml`}
\usage{
deploy_site(
  path = ".",
  progress = TRUE,
  trigger = TRUE,
  full_deploy = FALSE,
  ...
)
}
\arguments{
\item{path}{Chemin vers la racine du dépôt. Défaut `"."`.}

\item{progress}{Logique. Affichage de la progression. Défaut `TRUE`.}

\item{trigger}{Passé à [site2branch()] (hébergement OFCE uniquement).
Défaut `TRUE`.}

\item{full_deploy}{Passé à [site2branch()]. Défaut `FALSE`.}

\item{...}{Arguments supplémentaires passés à [site2branch()] le cas échéant.}
}
\value{
Invisible `NULL`.
}
\description{
Lit la clé `ofce_host` du `_quarto.yml` à la racine du dépôt. Si `TRUE`,
délègue à [site2branch()] (push de `_site` vers la branche de déploiement
FTP). Si `FALSE`, lance `quarto publish gh-pages`.
}
\seealso{
[site2branch()], [setup_site()]
}
