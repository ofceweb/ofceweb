% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deploy_pb.R
\name{deploy_pb}
\alias{deploy_pb}
\title{Déploie un policy brief (PB) OFCE}
\usage{
deploy_pb(
  path = ".",
  progress = TRUE,
  trigger = TRUE,
  full_deploy = FALSE,
  ...
)
}
\arguments{
\item{path}{Chemin vers la racine du dépôt. Défaut `"."`.}

\item{progress}{Logique. Affichage de la progression. Défaut `TRUE`.}

\item{trigger}{Passé à [site2branch()]. Déclenche le workflow GitHub Actions
FTP. Défaut `TRUE`.}

\item{full_deploy}{Passé à [site2branch()]. Défaut `FALSE`.}

\item{...}{Arguments supplémentaires passés à [site2branch()].}
}
\value{
Invisible `NULL`.
}
\description{
Équivalent PB de [deploy_wp()]. Route le déploiement selon l'état du registre
(`stage` dans `manifest.json`) et, avant publication, selon `stage-target`
dans `_quarto.yml` (positionné par [setup_pb()]) :
\itemize{
  \item **Publié** (`stage: FALSE`) : toujours vers FTP production
    (`ftp_deploy.yml`), quelle que soit la valeur de `stage-target`.
  \item **Non encore publié** (`stage: TRUE` ou `stage` absent) : destination
    lue depuis `stage-target` (`"auto"` réévalué à chaque appel selon le
    propriétaire GitHub actuel — `"ftp"` pour l'organisation `ofce`,
    `"gh-pages"` sinon ; `"ftp"` ou `"gh-pages"` forcent la destination).
}
}
\seealso{
[render_pb()], [site2branch()], [pb_version_up()], [pb_registry_request()]
}
