% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pb_redirect.R
\name{push_pb_staging_redirect}
\alias{push_pb_staging_redirect}
\title{Déploie la page de redirection stable pour le PB en staging}
\usage{
push_pb_staging_redirect(path = ".", progress = TRUE, trigger = TRUE)
}
\arguments{
\item{path}{Chemin vers la racine du dépôt. Défaut `"."`.}

\item{progress}{Logique. Affichage de la progression git. Défaut `TRUE`.}

\item{trigger}{Logique. Si `TRUE` (défaut), déclenche
`ftp_redirect_staging.yml` après le push.}
}
\value{
Invisible `NULL`. Appelée pour ses effets de bord.
}
\description{
Équivalent PB de [push_wp_staging_redirect()]. Génère un `index.html` de
redirection pointant vers la version courante d'un PB en staging FTP (lue
depuis `website.site-url` de `_quarto.yml`, de la forme
`https://staging.ofce.fr/{repo}/{version}/`), le pousse sur la branche
`site-staging-redirect`, puis déclenche le workflow
`ftp_redirect_staging.yml` pour publier la page à l'URL stable
`staging.ofce.fr/{repo}/`.
}
\details{
Met à jour la variable GitHub Actions `FTP_STAGING_REDIRECT_DIR` avec
`{repo}/` -- chemin relatif à la racine chroot `www/staging/` du compte
FTP `STAGING_USER` (même convention que `FTP_STAGING_DIR`, cf.
[setup_pb()]).

Ne fait rien si `website.site-url` n'a pas la forme attendue (PB publié,
brouillon GitHub Pages, ou brouillon sans version encore attribuée).
}
\seealso{
[deploy_pb()], [push_pb_redirect()], [pb_version_up()]
}
\keyword{internal}
