options(renv.config.auto.snapshot=TRUE,
        renv.config.dependencies.limit = Inf,
        renv.config.namespaces.check=FALSE)

options(sourcoise.init_fn = ofce::init_qmd,
        sourcoise.log = "OFF",
        sourcoise.src_in = "project")

source("renv/activate.R")

if(interactive()) {
  library(ofce)
  library(ofceweb)
