% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/site_redirect.R
\name{push_site_redirect}
\alias{push_site_redirect}
\title{Déploie la page de redirection vers la version courante d'un site OFCE}
\usage{
push_site_redirect(path = ".", progress = TRUE, trigger = TRUE)
}
\arguments{
\item{path}{Chemin vers la racine du dépôt. Défaut `"."`.}

\item{progress}{Logique. Affichage de la progression git. Défaut `TRUE`.}

\item{trigger}{Logique. Si `TRUE` (défaut), déclenche `ftp_redirect.yml`
après le push.}
}
\value{
Invisible `NULL`. Appelée pour ses effets de bord.
}
\description{
Génère un `index.html` de redirection pointant vers la version courante du
site (segment de version dans `site-path` du `_quarto.yml`), le pousse sur
la branche `site-redirect`, puis déclenche le workflow `ftp_redirect.yml`
pour le déployer à l'URL stable `www.ofce.fr/{site-path-sans-version}/`.
}
\details{
Ne fait rien si le `site-path` ne contient pas de segment de version
(`/v\d+`), ce qui correspond aux sites créés avec `versionning = FALSE`.

Met également à jour la variable GitHub Actions `FTP_REDIRECT_DIR` avec le
répertoire cible (chemin FTP sans préfixe de localisation ni version).
}
\section{Home Users}{

}

\seealso{
[setup_site()], [site_version_up()]
}
