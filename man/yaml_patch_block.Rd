% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/yaml_patch.R
\name{yaml_patch_block}
\alias{yaml_patch_block}
\title{Replace a whole structural value (list/sequence) at a dotted YAML path}
\usage{
yaml_patch_block(lines, path, value)
}
\arguments{
\item{lines}{Character vector of file lines.}

\item{path}{Dotted path to the key.}

\item{value}{An R list/vector to serialize as the new value, or `NULL`
to delete the key.}
}
\value{
The updated character vector of lines.
}
\description{
Serializes `value` via [yaml::as.yaml()] and splices it in place of the
existing subtree at `path` (or inserts it if absent), re-indented to
match. Comments *inside* the replaced subtree are necessarily lost (the
whole thing is regenerated); comments elsewhere in the file are
untouched. `value = NULL` deletes the key instead (see
[yaml_patch_delete()]).
}
